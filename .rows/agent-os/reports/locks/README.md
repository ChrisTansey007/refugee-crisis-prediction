# Lock Validation Reports

This directory contains lock validation reports generated by `scripts/validate-locks.mjs`.

Reports check lock JSON files in `agent-os/locks/` for required fields and expiration status.

Do not commit noisy generated reports unless they serve as useful starter examples.
