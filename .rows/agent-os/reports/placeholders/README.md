# Placeholder Audit Reports

This directory contains placeholder audit reports generated by `scripts/validate-placeholders.mjs`.

Reports distinguish between intentional template placeholders (e.g., `[PROJECT_NAME]`) and vague placeholders (e.g., `TODO`, `TBD`, `FIXME`).

Do not commit noisy generated reports unless they serve as useful starter examples.
